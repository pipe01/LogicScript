input one
input'2 two
output'7 out[5]

reg'4 register

assign out[0] = len(out) / len(out[0])
assign out[1] = len(one)
assign out[2] = len(two)
assign out[3] = len(register)

when *
    local $local'5
    out[4] = len($local)
end

@test (
    => out(5, 1, 2, 4, 5),
)
